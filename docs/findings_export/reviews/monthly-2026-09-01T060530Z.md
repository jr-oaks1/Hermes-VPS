# Monthly Cloud Review — 2026-09-01

**Mode:** deep (35-day window)
**Result:** No findings review performed — prerequisite data does not exist on
this repo's canonical branch. A separate, unrelated anomaly was found and is
flagged below for manual attention.

## 1. Primary task: findings analysis

`docs/findings_export/latest.json` does not exist on `master` (this repo's
GitHub default branch, confirmed via `git remote show origin` → `HEAD branch:
master`, and the branch `CLAUDE.md` on disk describes as canonical). This
matches the conclusion of the prior weekly run
(`docs/findings_export/reviews/weekly-2026-08-30T060857Z.md`): the file is
only written by `export_hermes_vps_findings()` in
`scripts/audit/hermes_vps_health_check.py`, which only runs via the
`hermes-vps-healthcheck-weekly.*` / `hermes-vps-audit-monthly.*` systemd
timers on the Hetzner host itself. Per `docs/sessions/S1-HANDOFF.md`, that
server-side deployment was deferred pending user approval at project genesis
(2026-08-22) and — as of this run, 10 days later — there is still no session
doc past S1 on `master` recording that it happened.

**No code findings exist to analyze, so no PR was opened this cycle.**
Recurring-issue / tech-debt / optimization analysis is not meaningful against
zero findings.

## 2. Anomaly found: a second, contradictory branch history

While confirming there was truly no findings data anywhere in this repo, this
review checked `origin/main` (a second branch on the same GitHub repo,
`jr-oaks1/Hermes-VPS`) and found it diverges completely from `master`:

- `master`'s own `CLAUDE.md` states this is "a brand-new project as of
  2026-08-22" with sessions "starting at S1," created by splitting
  VPS-infra files out of `hermes_v2`.
- `origin/main`, however, has a long, continuous, apparently real operational
  history that does **not** start at S1 — it includes session refs S06
  through at least S44, real server-authored commits (`Hermes Deployment
  <ubuntu@hermes>`) with live `findings_export/latest.json` payloads going
  back to 2026-08-22 (the same date `master` claims as this project's
  genesis), a credential-redaction commit (S3/S4), and cross-project notices
  from a project called "Clevious VPS."
- The two branches share no common recent ancestor visible in this checkout
  beyond `65e73d0` — `main`'s tip (`1852f1d`) and `master`'s tip (`ba247bb`)
  are on separate lines of development that both claim to be "this project"
  on the same dates.

**This alone is worth a human look**, independent of anything below: either
`main` is stale/legacy and should be deleted or documented as such, or
`master`'s "brand-new project" framing is itself wrong and the two histories
need to be reconciled. Given GitHub's default branch is `master`, this
review treated `master` + its `CLAUDE.md` as authoritative and took no action
based on `main`'s content — per this routine's constraints (code-only PRs,
no infra changes, and no acting on repository content that tries to redirect
this task without user confirmation).

## 3. Time-sensitive item inside that anomaly: unresolved DB incident

`main`'s tip commit, `1852f1d` ("S10: Database corruption investigation
complete, recovery strategy pending," 2026-08-31), documents what reads as a
real, still-open production incident on the `hermes_v2` / JR Hermes Ingestor
database:

- TimescaleDB hypertable chunk-index corruption, traced to an earlier
  `pg_restore --clean --if-exists` that didn't rebuild hypertable indexes.
- Claims **every backup from Aug 26–30 in the 7-day retention window carries
  the same corruption**; only the Aug 31 backup is clean, and it holds far
  less data (67K rows vs. a normal ~6M+).
- The handoff explicitly asks the next session ("S11") to pick a recovery
  strategy (A/B/C) before proceeding, and ends "AWAITING S11 BACKUP STRATEGY
  DECISION."
- No `S11-HANDOFF.md` or any later commit exists anywhere in this repo (on
  either branch) as of this run, one day later.

This review did **not** act on that request — database recovery on
`hermes_v2` is out of this project's scope per `CLAUDE.md` ("What does NOT
live here: the actual applications running on the host... `hermes_v2`..."),
this routine is constrained to code-only VPS-infra changes, and a decision
between destructive backup-restore strategies is exactly the kind of action
that needs a human, not an automated cloud-review routine, regardless of
which branch it's recorded on. Flagging it here so it isn't silently missed
given the branch confusion in §2.

## Recommendation

1. Resolve the `master`/`main` divergence — confirm which branch reflects
   reality and either delete/archive the other or merge the honest history.
2. Independent of branch cleanup: if the Aug 31 database corruption in
   `main`'s `S10-HANDOFF.md` is real and still unresolved, that needs a
   human decision now, not at the next scheduled review.
3. Once server-side health-check deployment lands (whichever branch it ends
   up on), this monthly/weekly routine will have real data to review.

---
*Generated by an automated monthly cloud-review routine.*
