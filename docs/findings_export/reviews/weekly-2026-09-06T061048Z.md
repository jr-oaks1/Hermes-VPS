# Weekly Cloud Review — 2026-09-06

**Mode:** quick (8-day window, 2026-08-30 → 2026-09-06)
**Source:** `docs/findings_export/latest.json` (`generated_at`
2026-09-06T04:00:05Z, 71 entries, ids 105–178)
**Result:** No code fix opened this cycle — every finding in the window is
either informational or already resolved as of the most recent checks.

## Branch note

This review was run against `origin/main` (`ac928ec`, through S14), which is
where all real session history and the findings export live. The local
`master` branch on this host was still pinned at S1 (pre-server-deployment)
before this run. See **Flagged for manual review** below.

## Summary of the window

The 8-day window spans two very different states of the host:

- **2026-08-30 → 2026-09-06 01:17** (ids 105–154): every quick/deep check in
  this span reported two recurring **critical** findings:
  - `service.hermes_v2: inactive`
  - `api.health: ok` / `degraded` with `unhealthy agents: {'fred_data': 'disabled', ...}`
  plus, on the 2026-09-01 and 2026-09-02 deep checks, **warning**-level git
  drift (`git.hermes_v2` and `git.hermes-vps` local HEAD != `origin/main`).
- **2026-09-06 03:25 onward** (ids 155–178): all of the above are gone. Every
  finding in this later span is `severity: info` — services active, API
  healthy (11 agents), replication caught up, backups fresh, git in sync.

## Why no action was needed

Cross-referencing `docs/sessions/S13-HANDOFF.md` and `S14-HANDOFF.md` (both
dated 2026-09-06, landed between the 01:17 and 03:25 checks in this window):

1. **`service.hermes_v2: inactive`** was a false CRITICAL, not a real outage —
   the health check's `SYSTEMD_SERVICES` list still named the decommissioned
   `hermes_v2` unit instead of its rename, `hermes-ingestor`. Fixed in S13
   (`a3cc7d2`).
2. **`fred_data: disabled`** was flagged as unhealthy by `check_api_health`,
   but `disabled` is an intentional operator choice (confirmed live in S14's
   post-reboot verification pass), not a fault. S13 added `disabled` to the
   benign-states set in the same commit.
3. **git drift warnings** (`git.hermes_v2`, `git.hermes-vps` HEAD mismatches)
   were resolved by S13's `_sync_repo_to_origin()` self-healing step and the
   removal of `check_git_sync('/opt/hermes_v2')` entirely (that repo is
   decommissioned).

In other words, the one substantive code-level bug visible in this window —
the health check's own false-CRITICAL logic — was already fixed by the two
sessions that ran inside this same 8-day window, before this review executed.
There is nothing left in `latest.json` to open a PR for.

## Flagged for manual review (not actioned — outside this routine's scope)

- **`master`/`main` divergence on this repo.** `master` is 4 commits behind
  `main` (stuck at S1: `37e851e`..`ba247bb`), while `main` carries all real
  work through S14 (`ac928ec`), including the findings-export pipeline this
  review depends on. A prior automated review run already flagged this
  (`200f35c`, monthly review commit) but it hasn't been resolved. Deciding
  whether to fast-forward/delete `master` or repoint the repo's default
  branch is a repository-administration call, not a code fix — flagging for
  the user rather than acting unilaterally on a shared branch.
- **Minor cosmetic finding, id 166** (2026-09-06 03:25 deep check):
  `git.hermes-vps: in sync with origin, uncommitted local changes present`
  with detail `?? logs/`. `logs/` has been in `.gitignore` since S13
  (`a3cabe4`), committed as part of the same sync that this check reports as
  already in sync with `origin/main` — so an untracked `logs/` should not
  normally surface via `git status --short`. This is state on the live
  `/opt/hermes-vps` deploy checkout on the Hetzner host, which this routine
  has no access to and won't speculate about further; severity is `info`
  only (no functional impact), so it's noted here for whoever next has a
  shell on the host to glance at, not urgent.

## Constraints observed

Per this routine's standing instructions: no infra config (nginx,
Prometheus, firewall, DB) was touched, no code fix was pushed directly to
`main`, and no PR was opened since none of the window's findings had an
outstanding code-only fix available.

---
*Generated by an automated weekly cloud-review routine.*
